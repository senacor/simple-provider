
spring:
  main:
    banner-mode: off

  datasource:
    url: jdbc:postgresql://localhost:5432/
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties.hibernate.format_sql: true
    hibernate:
      generated: drop-and-create
  